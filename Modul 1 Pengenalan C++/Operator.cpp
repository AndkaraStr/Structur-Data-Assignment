#include <iostream>
using namespace std;

int main () {
    int a;
    int b;

    cout << "Masukan Angka";
    cin >> a;
    cout << "Masukan Angka";
    cin >> b;

    //operator Aritmatika
    cout << "a + b = " << (a+b) << endl;
    cout << "a - b = " << (a-b) << endl;
    cout << "a * b = " << (a*b) << endl;
    cout << "a / b = " << (a/b) << endl;
    cout << "a % b = " << (a%b) << endl;
    
    return 0;
}